{
  "name": "hepsiburada-puppeteer-cookies",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-deps": "apt-get update && apt-get install -y chromium-browser"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer-core": "^21.0.0",
    "axios": "^1.5.0"
  }
}
