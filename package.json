{
  "name": "hepsiburada-playwright-cookies",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-deps": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright-core": "^1.40.0",
    "axios": "^1.5.0"
  }
}
